{% extends 'base.html.twig' %}

{% block title %}PetStore{% endblock %}

{% block body %}
    <h1>PetStore</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pet_store.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pet_store.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ pet_store.surname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pet_store_index') }}">back to list</a>

    <a href="{{ path('pet_store_edit', {'id': pet_store.id}) }}">edit</a>

    {{ include('pet_store/_delete_form.html.twig') }}
{% endblock %}
